import { SearchInput } from "@/entities/stream";

export const Search = () => {
    return (
        <div>
            <SearchInput
                isResultsShowing={false}
                onChange={() => {}}
                onClick={() => {}}
                onKeyDown={() => {}}
            />
        </div>
    );
};
